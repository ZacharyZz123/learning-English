# 自然拼读质量报告

- 总词数：2519
- ERROR：0
- WARN：467
- 问题清单：`E:\xuexi\learning-English\词汇\单词\00_自然拼读质量问题清单.csv`

## 规则统计

- `risky_word_without_sounds`：290
- `weak_visual_split`：120
- `high_risk_pattern_split`：42
- `ed_not_grouped`：10
- `ing_not_grouped`：5

## 前 30 条问题

- `WARN` `advantage`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `again`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `although`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `Amusement`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `angel`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `angry`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `anybody`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `anyone`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `anything`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `anyway`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `are`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `area`：高风险拼读组合 `ea` 没有作为整体分块。
- `WARN` `area`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `aren't`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `Asian`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `bamboo`：高风险拼读组合 `mb` 没有作为整体分块。
- `WARN` `bandage`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `baseball`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `basin`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `beautiful`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `beautifully`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `beauty`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `behaviour`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `behaviour`：连续单字母视觉拆分过长，教学价值低。
- `WARN` `between .`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `biscuit`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `blackboard`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `blood`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `board`：包含高风险拼读组合，但没有可靠分块音标。
- `WARN` `boarding`：包含高风险拼读组合，但没有可靠分块音标。

## 说明

- `ERROR` 表示数据或重点回归样本必须修复。
- `WARN` 表示自然拼读教学质量可疑，需要逐批补规则或覆盖表。
- 本检查不等于人工词典校对，只负责发现高风险拆分和对齐问题。